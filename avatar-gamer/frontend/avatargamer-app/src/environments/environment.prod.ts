export const environment = {
  production: true,
  apiUrl: 'http://localhost:8000/api/v1',  // Reemplazado con tu dirección IP local para pruebas
  socketUrl: 'http://localhost:8000'  // Para producción real, usa la URL del servidor Socket.IO
  // Para producción real, usa la URL de tu servidor de producción
};
