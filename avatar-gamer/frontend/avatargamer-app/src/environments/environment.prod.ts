export const environment = {
  production: true,
  apiUrl: '/api/v1',  // Reemplazado con tu dirección IP local para pruebas
  socketUrl: 'https://capstone-steel-delta.vercel.app'  // Para producción real, usa la URL del servidor Socket.IO
  // Para producción real, usa la URL de tu servidor de producción
};
